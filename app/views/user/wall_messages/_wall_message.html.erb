<% wall = wall_message.commentable %>
<% type = wall.class.to_s.underscore %>
<div class="feed-img-list" id='comment_<%= wall_message.id %>'>
  <div class="feed-list">
  <div class="feed-img"><%= avatar wall_message.poster %></div>
    <p>
      <%= profile_link wall_message.poster %> 
      <%= ftime wall_message.created_at %>
      <% if wall_message_repliable_by_current_user message %>
        <%= link_to_function '回复', "Iyxzone.WallMessage.set('#{wall_message.poster.login}');" %>
      <% end %>
    </p>
    <p><%= wall_message.content  %></p>
  </div>
  <% if wall_message_deleteable_by_current_user message %>
    <%= link_to_remote 'x', :url => eval("#{type}_wall_message_url(wall, wall_message)"), :method => :delete, :class => 'icon-active' %>
  <% end %>
</div>

