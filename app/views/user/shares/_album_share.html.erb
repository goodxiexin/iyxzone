<% album = share.shareable %>
<% first_sharer = album.first_sharer %>
<% poster = sharing.poster %>
<div class="feed-list feed-share">
  <%= icon_dig_link share %>
  <div class="feed-header">
    <div class="right">
      <%= ftime share.created_at %><br/>
      首次分享人：<%= avatar first_sharer, :with => '27px', :height => '27px' %>
			<%= profile_link first_sharer %>
    </div>
    <h3>相册: <%= link_to h(sharing.title), eval("#{album.class.name.underscore}_url(album)"), :popup => true %></h3>
    <p class="con space">
      <span class="share-album-cover"><%= album_cover album, :class => 'imgbox01', :size => :large, :width => 75, :height => 75, :popup => true %></span><br/>
    </p>
    <%= sharing_reason sharing %>
    <div class="feeds">
      <%= link_to_function "评论(#{sharing.comments_count})", "iyxzone.comment.togglebox('sharing', #{sharing.id}, #{sharing.comments_count})" %>
      <% if !is_mine %> 
        | <%= facebox_link "我也分享", new_sharing_url(:url => eval("#{album.class.name.underscore}_url(album)")), :width => 450 %><span>(<%= album.sharings_count %>)</span> 
        | <%= link_to "更多分享", shares_url(:uid => poster.id) %>
      <% end %>
    </div>
  </div>
  <div id="sharing_comment_box_<%= sharing.id %>" style="display: none">
    <%= render :partial => 'user/comments/comment_box', :locals => {:commentable => sharing, :recipient => poster} %>
  </div>
</div>
