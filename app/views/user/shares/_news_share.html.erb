<% news = share.shareable %>

<% render :layout => 'share_item', :locals => {:sharing => sharing, :share => share, :shareable => news} do %>
  <h3>新闻：<%= link_to h(sharing.title), news_url(news), :popup => true %></h3>
  <%= sharing_reason sharing %>
<% end %>
