<div id="canvas" class="round_r_t"><div class="round_l_t"><div class="round_r_b"><div class="round_l_b">
  <div class="round_m"><div class="round_body">
    <h1 class="app-cons">公会</h1>
    <div class="box02 canvas_int">
	    <div class="tab tab01">
        <div class="icon">
          <%= link_to "新建工会", new_guild_url, :class => "icon-add-table" %>
        </div>
		  <% subject = get_subject @user %>
		    <ul>
			    <li><span><%= link_to "热门工会", hot_guilds_url(:id => @user.id) %></span></li>
			    <li><span><%= link_to '最新工会', recent_guilds_url(:id => @user.id) %></span></li>
			    <li class='hover'><span><%= link_to "#{subject}的工会", guilds_url(:id => @user.id) %></span></li>
        <% if @user == current_user %>
			    <li><span><%= link_to "好友工会", friends_guilds_url %></span></li>
        <% end %>
		    </ul>
      </div>
	    <div class="piclist-rows">
        <div class='nav-link'>
          <ul class='s_clear'>
            <li class='hover'><%= link_to "<span>#{subject}发起的(#{@user.guilds_count})</span>", guilds_url(:id => @user.id) %></li>
            <li><%= link_to "<span>#{subject}参加的(#{@user.participated_guilds_count})</span>", participated_guilds_url(:id => @user.id) %></li>
          </ul>
        </div>
		    <%= render :partial => 'user/guilds/guild_items', :object => @guilds %>
      </div>
    </div>
  </div></div>
</div></div></div></div>
