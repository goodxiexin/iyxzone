        <div class='nav-link'>
          <ul class='s_clear'>
            <li <% if hover==0 %>class='hover'<% end %>><%= link_to "<span>#{get_subject user}发起的(#{user.guilds_count})</span>", guilds_url(:uid => user.id) %></li>
            <li <% if hover==1 %>class='hover'<% end %>><%= link_to "<span>#{get_subject user}参加的(#{user.participated_guilds_count})</span>", participated_guilds_url(:uid => user.id) %></li>
          </ul>
        </div>

