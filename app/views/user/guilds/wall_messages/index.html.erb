<div class='box02 canvas_con'>
  <div class='path-box'>
    <label>工会"<%= guild_link @wall %>"</label>
    <span>留言版</span>
  </div>
  <% if @can_reply %>
    <%= render :partial => 'user/wall_messages/wall', :locals => {:wall => @wall, :recipient => @wall.president} %>
  <% else %>
    <%= "你必须加入工会才能留言，点击#{ link_to '这里', guild_url(@guild) }查看活动详情" %>
  <% end %>
  <div class="feed-box" id='comments'>
    <%= render :partial => 'user/wall_messages/wall_message', :collection => @messages %>
  </div>
  <%= will_paginate @messages %>
</div>
