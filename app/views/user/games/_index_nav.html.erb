        <div class='nav-link'>
          <ul class="s_clear">
            <li <% if hover==0 %>class='hover'<% end %>><%= link_to "<span>#{get_subject user}玩的游戏(#{user.games_count})</span>", games_url(:uid => user.id) %></li>
          </ul>
        </div>

