<div id="canvas" class="round_r_t"><div class="round_l_t"><div class="round_r_b"><div class="round_l_b">
        <div class="round_m"><div class="round_body">

<h1 class="big-photo">照片</h1>
<div class="box02 canvas_int">
	<div class="tab tab01">
		<% if @user == current_user %>
		<div class="right">
			<%= link_to "上传照片", select_personal_albums_url(:id => @user.id) %>
			<%= link_to "新建相册", new_personal_album_url, :rel => 'facebox' %>
    </div>
		<% end %>
    <ul>
      <li><span><%= link_to "热门照片", hot_personal_photos_url(:id => @user.id) %></span></li>
			<li><span><%= link_to '最新照片', recent_personal_albums_url(:id => @user.id) %></span></li>
			<li class='hover'><span><%= link_to "游戏#{@game.name}的相册", game_albums_url(@game) %></span></li>
    </ul>				
	</div>
  <div class="box02 appcontent01">
    <div class="piclist s_clear">
			<%= render :partial => 'personal_album/albums/album_item', :collection => @albums %>
    </div>
  </div>
</div>

<%= will_paginate @albums %>

</div></div>
</div></div></div></div>

