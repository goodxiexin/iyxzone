<div id="canvas" class="round_r_t"><div class="round_l_t"><div class="round_r_b"><div class="round_l_b">
  <div class="round_m"><div class="round_body">
    <h1 class="app-vote">投票</h1>
    <div class="box02 canvas_int">
      <div class="tab tab01">
        <div class="right"><%= link_to "新投票", new_poll_url %></a></div>
        <ul>
          <li><span><%= link_to "热门投票", hot_polls_url(:id => @user.id) %></span></li>
          <li><span><%= link_to "最近投票", recent_polls_url(:id => @user.id) %></span></li>
          <li class='hover'><span><%= link_to "#{get_subject @user}的投票", polls_url(:id => @user.id) %></span></li>
          <% if @user == current_user %>
          <li><span><%= link_to "朋友的投票", friends_polls_url(:id => @user.id) %></span></li>
          <% end %>
        </ul>
      </div>
      <div class="vote-box">
        <div class="nav-link">
          <ul class="s_clear">
            <li><%= link_to "<span>#{get_subject @user}发起的投票</span>", polls_url(:id => @user.id) %></li>
            <li class='hover'><%= link_to "<span>#{get_subject @user}参与的投票</span>", participated_polls_url(:id => @user.id) %></li>
          </ul>
        </div>
        <%= render :partial => 'poll_items', :object => @polls %>
        <div class="pages">
          <a href="#">下一页</a>
          <a href="#">5</a>
          <a href="#">4</a>
          <a href="#">3</a>
          <a href="#">2</a>
          <a class="hover" href="#">1</a>
        </div>
      </div>
    </div>
  </div></div>
</div></div></div></div>
