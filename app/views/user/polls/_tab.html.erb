      <div class="tab tab01">
        <div class="right"><%= link_to "新投票", new_poll_url %></a></div>
        <ul>
          <li <% if hover==0 %>class='hover'<% end %>><span><%= link_to "热门投票", hot_polls_url %></span></li>
          <li <% if hover==1 %>class='hover'<% end %>><span><%= link_to "#{get_subject user}的投票", polls_url(:uid => user.id) %></span></li>
          <% if user == current_user %>
          <li <% if hover==2 %>class='hover'<% end %>><span><%= link_to "朋友的投票", friends_polls_url %></span></li>
          <% end %>
        </ul>
      </div>

