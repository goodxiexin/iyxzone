<% content_for :html_title, "#{current_user.login} - 好友活动 - 17Gaming.com" %>
<% content_for :html_description, "#{current_user.login}的好友活动 - 17Gaming.com (一起游戏网)愿做网络游戏玩家的精神家园。" %>
<% content_for :html_keywords, "#{current_user.login}, 游戏活动, 17Gaming, 一起游戏, 一起游戏网, 网络游戏玩家, 游戏社区, 社交网站, 网络游戏社区" %>

<div id="canvas" class="round_r_t"><div class="round_l_t"><div class="round_r_b"><div class="round_l_b">
  <div class="round_m"><div class="round_body">
    <h1 class="app-ploy">活动</h1>
    <div class="box02 canvas_int">
	    <div class="tab tab01">
        <div class="icon">
          <%= link_to "新建活动", new_event_url, :class => 'icon-add-table' %>
        </div>
		    <ul>
			    <li><span><%= link_to "热门活动", hot_events_url(:id => current_user.id) %></span></li>
			    <li><span><%= link_to "我的活动", events_url(:id => current_user.id) %></span></li>
			    <li class='hover'><span><%= link_to "朋友的活动", friends_events_url %></span></li>
		    </ul>
      </div>
      <div class="piclist-rows" id='events'>
        <% if @events.total_entries != 0 %>
	        <%= render :partial => 'event_items', :object => @events %>
        <% else %>
          <div class='gray'>你的好友最近都人间蒸发了，没人创建活动</div>
        <% end %>
      </div>
    </div>
  </div></div>
</div></div></div></div>
