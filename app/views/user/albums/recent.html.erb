<div id="canvas" class="round_r_t"><div class="round_l_t"><div class="round_r_b"><div class="round_l_b">
  <div class="round_m"><div class="round_body">
    <h1 class="app-photo">照片</h1>
    <div class="box02 canvas_int">
	    <div class="tab tab01">
		    <div class="right">
		      <%= link_to "上传照片", select_personal_albums_url %>
		      <%= facebox_link "新建相册", new_personal_album_url %>
        </div>
        <ul>
          <li class='hover'><span><%= link_to "热门照片", hot_personal_photos_url %></span></li>
          <li><span><%= link_to "我的相册", personal_albums_url(:uid => current_user.id) %></span></li>
          <li><span><%= link_to "圈了我的照片", relative_personal_photos_url(:uid => current_user.id) %></span></li>
					<li><span><%= link_to '好友相册', friends_personal_albums_url %></span></li>
        </ul>				
	    </div>
      <div class="piclist-rows">
        <div class="nav-link">
          <ul class="s_clear">
            <li><%= link_to "<span>人气最高</span>", hot_personal_photos_url %></li>
            <li class='hover'><%= link_to "<span>最新上传</span>", recent_personal_albums_url %></li>
          </ul>
        </div>
        <% if @albums.total_entries != 0 %>
        <h3>最近共更新 <span><%= @albums.total_entries %></span> 个相册</h3>
          <table class='big' cellpadding="0" cellspacing="1">
            <% @albums.each do |album| %>
            <tr>
              <td>
                <div class="imgwrap">
                    <%= album_cover album, :size => :large, :class => 'imgbox01' %>
                </div>
                <div class="data">
                  <h3><%= profile_link album.poster%>的<%= album_link album %>共<span><%= album.photos_count %></span>张</h3>
                  <p>创建于<%= ftime album.created_at %></p>
                  <p>更新于<%= ftime album.uploaded_at %></p>
                </div>
              </td>
            </tr>
            <% end %>
					</table>
          <%= will_paginate @albums %>
        <% else %>
          <div class='tip'>最近大家都没有更新什么相册，你赶紧上传些图片吧</div>
        <% end %>
      </div>
    </div>
  </div></div>
</div></div></div></div>
