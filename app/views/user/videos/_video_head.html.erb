        <div class='detail-video-head jl-cutline s_clear'>
          <p class="left"><%= h video.title %> <span class='gray'><%= ftime video.created_at %></span></p>
          <div class="topIsay right"><span class="button04"><span><%= facebox_button "我来说说", new_mini_blog_url(:content => "#{video.title} #{SITE_URL}/videos/#{video.id}"), :width => 500 %></span></span></div> 
          <p class="right">
            <% if user == current_user %>
              <%= link_to "<span class='icon2-edit'></span>编辑", edit_video_url(video), :class => 'red' %>
              <%= link_to_function "<span class='icon2-close'></span>删除", "Iyxzone.Video.confirmDeletingVideo(#{video.id}, #{user.id});", :class=>'red w-l' %>
            <% end %>
          </p>
        </div>

