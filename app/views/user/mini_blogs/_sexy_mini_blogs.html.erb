<%= render :partial => 'sexy_mini_blog', :collection => mini_blogs, :locals => {:at => at} %>

<% if @remote.nil? %>
  <%= will_paginate mini_blogs %>
<% else %>
  <%= remote_will_paginate mini_blogs, :remote => @remote %>
<% end %> 
