<% if @setting.email_viewable_by? current_user %>
<div class="rows s_clear">
  <div class="id">email：</div>
  <div class="left"><%= @user.email %></div>
</div>
<% end %>
<% if @setting.qq_viewable_by? current_user %>
<div class="rows s_clear">
  <div class="id">qq：</div>
  <div class="left"><%= @profile.qq %></div>
</div>
<% end %>
<% if @setting.phone_viewable_by? current_user %>
<div class="rows s_clear">
  <div class="id">联系电话：</div>
  <div class="left"><%= @profile.phone %></div>
</div>
<% end %>
<% if @setting.website_viewable_by? current_user %>
<div class="rows s_clear">
  <div class="id">个人网站：</div>
  <div class="left"><%= @profile.website %></div>
</div>
<% end %>

